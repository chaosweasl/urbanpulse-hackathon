// Messages: MessageBubble — single message in a conversation
// TODO: Sender-aligned bubble with content, timestamp, read status
export function MessageBubble() {
  return <div>{/* TODO */}</div>;
}
