"use client";
// Shared: SearchBar — universal search input with debounce
// TODO: Input with search icon, debounced onChange, optional category filter
export function SearchBar() {
  return <div>{/* TODO */}</div>;
}
