// Shared: AvatarWithBadge — user avatar with optional verified/role badge
// TODO: Avatar image with fallback initials, overlay badge for verified users
export function AvatarWithBadge() {
  return <div>{/* TODO */}</div>;
}
