// Map: PulseMarker — marker for a pulse on the map
// TODO: Custom marker icon per pulse type, popup with details
export function PulseMarker() {
  return null; // Rendered by Leaflet, not as normal JSX
}
