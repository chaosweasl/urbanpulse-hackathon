// Feed: WeatherAlert — pinned severe weather banner
// TODO: Fetch weather data, show alert if severe, pin Safety Check-in thread
export function WeatherAlert() {
  return <div>{/* TODO */}</div>;
}
