"use client";

// Layout: MobileNav — bottom navigation bar for mobile
export function MobileNav() {
  return (
    <nav className="fixed bottom-0 left-0 right-0 border-t bg-background md:hidden">
      {/* TODO: Bottom tab bar: Feed, Map, +Create, Messages, Profile */}
    </nav>
  );
}
