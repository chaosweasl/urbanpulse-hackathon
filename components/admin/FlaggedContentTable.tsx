// Admin: FlaggedContentTable — table of reported/flagged content
// TODO: Filterable table with report reason, reported item, actions (resolve, dismiss)
export function FlaggedContentTable() {
  return <div>{/* TODO */}</div>;
}
